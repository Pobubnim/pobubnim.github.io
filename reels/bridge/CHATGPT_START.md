# ChatGPT, начни отсюда

Репозиторий: Pobubnim/pobubnim.github.io.
Рабочая ветка рилсов (читать отсюда): `claude/blog-setup-chatgpt-images-8jobkr`.
Твоя ветка (писать сюда): `codex/reels-images`, создать от рабочей ветки.

1. Прочитай `reels/bridge/README.md` (роли и правила) и `reels/bridge/STATUS.md`.
2. Для каждой строки со статусом `ждёт картинок` или `переделать sN` открой её
   задание в `reels/bridge/tasks/` и нарисуй кадры. Промпт копируй из задания целиком,
   вместе со строкой `Style:`, чтобы серия выглядела одинаково.
3. Сохрани кадры в `reels/incoming/<slug>/<id>.jpg` (вертикаль 2:3, до 1,5 МБ).
4. В `STATUS.md` поменяй статус на `картинки сданы`, в заметке перечисли файлы.
5. Закоммить и запушь `codex/reels-images`. Открой PR в рабочую ветку рилсов
   с заголовком «Картинки для рилсов: <slugs>».
6. Больше ничего в репозитории не меняй.
