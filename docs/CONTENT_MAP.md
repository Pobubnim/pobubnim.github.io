# Карта контента ПОБУБНИМ

## Откуда что берётся
- Фото: C:\Users\User\Pictures\Материалы для Побубним → tools/prepare_media.py → assets/img/*.webp
  Слаги и исходники — в PHOTOS внутри скрипта. Тройники tri-* НЕ РЕЗАТЬ — коллажи 3 кадров, только целиком.
- Hero-видео: «Цвет ДО-ПОСЛЕ … LOOP видео.mp4» (4K 118с) → assets/video/hero-loop.mp4 (1080p x264 CRF28, без звука).
- Видеоработы: ВИДЕО хотлинком на https://seversvet.github.io/assets/film/<id>[-loop].mp4,
  а ОБЛОЖКИ с 02.09 свои — assets/img/film/<id>.webp (40 файлов, 1,6 МБ). Проверено
  блокировкой домена: раньше падение соседней площадки оставляло сетку битых
  картинок, теперь портфолио выглядит целым, теряется только оживление и плеер.
  Каталог работ на той стороне — seversvet site/assets/portfolio.js.
- Скрины приложения MONOLITH: хотлинк https://monolithapp.github.io/assets/desktop-*.jpg, mobile-*.jpg.
- Свои фильмы витрины: FTX13 → assets/video/ftx.mp4 (мотофристайл), «Поля инста» →
  polya.mp4 (fashion) + лупы и постеры; конвейер `python tools/prepare_media.py films`.
- Hero: hero-loop-hd.mp4 (1440p, ПК ≥1024px) + hero-loop-m.mp4 (720p, телефон); выбирает
  инлайн-скрипт в разметке ДО загрузки. Обложка так же по ширине: hero-poster.webp
  (1920px, 143 КБ) и hero-poster-m.webp (828px, 39 КБ, режется из первой). В фоне
  секции .hero лежит 240-байтная затравка кадра прямо в style.css — первый экран не
  чёрный, пока идёт обложка. hero-loop.mp4 (1080p, 38,6 МБ) остался ТОЛЬКО у видео
  в секции «Цвет» на ПК; телефон и там берёт копию 720p.

## Темы фотоматериала
Тройники: завод-BTS ×8, гонки ×2, свадьбы ×2, город ×2, клипы (балет, микрофон, силуэт, тёмный портрет),
предметка (телефоны ×2, бар), концерт, кабаре, бокс, поля ×2, кино-портреты, арт (красное пальто ч/б).
Горизонтальные: часы ×2, talking head ×2, грим (циркач, инопланетянин), тёплый портрет, ч/б BTS, венок 4K, свадьба.
Обо мне: за монитором (about-monitor), с камерой (about-camera), стедикам, риг, хромакей, фишай.

## Служебная графика (генерируется, не рисуется руками)

- OG-превью страниц: `python tools/build_og.py` (кадр + скрим + подпись) и
  `python tools/build_og_tools.py` (карточки инструментов: тёмный фон, название,
  мини-лист с примером). Результат — assets/og/*.jpg, у инструментов префикс tool-.
- Шрифты сайта лежат у нас: `python tools/fetch_fonts.py` тянет woff2-подмножества
  и пересобирает assets/css/fonts.css. Google Fonts со страниц убран (кроме архивной
  videos/log-i-grade). Меняете веса в CSS — перегоняйте скрипт.

## Правила
- Тяжёлые исходники в git не кладём; видео работ не дублируем — хотлинк.
- Новые фото: добавить строку в PHOTOS → python tools/prepare_media.py → закоммитить webp.

## Глубина страниц (21.09.2026)

Замер `index_status.py` показал: в поиске 20 адресов из 56, и выброшено ровно то,
что пересказывает общеизвестное. Инструменты при этом держатся. Отсюда правило
для любого нового и старого текста на сайте:

**Страница обязана нести то, чего нет у конкурента.** Три источника такого материала,
по убыванию силы:
1. **Факты ремесла** — физика света и звука, как устроен процесс, почему так, а не иначе.
   Проверяемы, бесплатны и не копируются: чтобы их написать, надо снимать.
2. **Свои инструменты** — врезка `.tool-hint` со ссылкой в наш калькулятор или конструктор
   по смыслу страницы. Ни у одного конкурента этого нет вовсе.
3. **Честная граница** — блок `.honest` «что считается отдельно», «что автоматизировать
   не стоит», «чего курсы не дают». Единственный раздел, который отговаривает от покупки,
   и поэтому ему верят.

Чего в этих разделах быть НЕ должно: выдуманных цен (единственный источник —
`assets/data/prices.json`), выдуманных сроков и бизнес-правил, марок техники, которой
у нас нет, чужих отзывов и кейсов (38-ФЗ).

Готовые блоки под это лежат в `assets/css/style.css` ОДИН раз, а не копией в каждой
странице: `.svc-sub .cmp-wrap .cmp .cmp-note .facts .honest .tool-hint`. Таблица
`.cmp` обязана жить в обёртке `role="region" tabindex="0" aria-label="…"`, иначе
axe-core краснеет на прокручиваемой области, а всей цепочке предков нужен `min-width: 0`,
иначе таблица рвёт страницу на 375 px вместо того, чтобы листаться.

## Разделы-хабы (28.08.2026)

- `articles/index.html` — статьи: разборы цен, «как устроено», DIY-гайды.
  Свежая (29.08): `blyoklaya-kartinka.html` — почему кадр серый и что чинится
  цветом; ведёт в приборы онлайн и в урок про log.
- `uroki/index.html` — уроки: карта интерфейса DaVinci («где экспозиция», «где
  баланс белого», «как включить скоупы» — факты по EDU_BASE §1, §2, §8е.6),
  восемь уроков двумя блоками (цвет · камера и оптика), FAQ. До 28.08 адреса
  `/uroki/` не существовало — GitHub Pages отдавал 404, а восемь сильных страниц
  висели без узла.
- `instrumenty/index.html` — семнадцать карточек пятью группами (24.09; было тринадцать
  тремя группами), таблица категорий ниже.
- `ceny.html` (17.09) — прайс всех услуг из `assets/data/prices.json` + подбор формата;
  `video-dlya-biznesa.html` (17.09) — задачи компаний → форматы; обложки работ из
  `assets/img/film/` (evergo, balabanovo, finopolis, ai-premium) и `polya-poster.webp`.

Хабы связаны крест-накрест, пункт «Уроки» добавлен в обе навигации через
`assets/js/nav.js` (шапки статические в 50 файлах — правится одним местом).

## Свадебные работы (28.08.2026)

Добавлены два фильма, оба хостятся в репо seversvet (`site/assets/film/`),
на pobubnim идут хотлинком — канон «видео работ не дублируем»:

- `wedding-td` — «Тимур и Дилия», 3:12. Исходник 1920×1080 с леттербоксом,
  реальный кадр 2.4:1 — полосы срезаны кропом `crop=1920:800:0:140`, иначе
  в карточке была бы рамка в рамке. Веб-копия 1280×534, CRF 23, ~29 МБ.
- `wedding-sd` — «Саша и Даня», 3:36. Исходник 4K HEVC 690 МБ, кадр 16:9 без
  полос. Веб-копия 1280×720, CRF 26 + maxrate 2 Мбит/с (при CRF 23 файл весил
  69 МБ — вдвое против эталона `wedding-de`).

Профиль веб-копии: `libx264 -crf 23…26 -preset medium -pix_fmt yuv420p
-movflags +faststart`, звук AAC 128k. Петля для наведения — 6 секунд,
ширина 720, без звука, CRF 30 (~160 КБ). Постер — webp качества 82 из
выразительного кадра со светом и лицами: на тёмном сайте карточка должна
быть светлым пятном.

## Работы 01.09.2026 — четыре новых фильма (профиль «высокое качество»)

Добавлены `terra`, `heleo`, `rskg`, `uzhasno`. Файлы, как и раньше, лежат в
репо seversvet (`site/assets/film/`), на pobubnim идут хотлинком.

По слову владельца планка пережатия поднята: «сжато, но без потери качества,
чтобы не рассыпалось на пиксели». Прежние работы жались до 720p и 0,6–0,9 Мбит/с
(vr2-review — 780 кбит/с) — оттуда и «пиксели». Новый профиль:

```
-vf "scale=1920:-2:flags=lanczos,format=yuv420p"
-c:v libx264 -preset slow -crf 20 -tune film -profile:v high -level 4.1
-x264-params "aq-mode=3:psy-rd=1.00,0.15:bframes=5:ref=5"
-movflags +faststart -c:a aac -b:a 192k -ar 48000
```

| id | что | длина | кадр | вес | битрейт |
|---|---|---|---|---|---|
| terra | реклама ресторана Terra | 0:15 | 1920×576 | 4,0 МБ | 2,1 Мбит/с |
| uzhasno | клип King Steve Benjamin | 2:08 | 1920×1080 | 88,1 МБ | 5,5 Мбит/с |
| rskg | кольцевые гонки РСКГ | 1:06 | 1920×1080 | 64,4 МБ | 7,8 Мбит/с |
| heleo | клиника косметологии | 7:37 | 1920×1080 | 90,6 МБ | 1,6 Мбит/с |

**ЛИМИТ GITHUB — 100 МБ НА ФАЙЛ, он и определяет потолок длинных роликов.**
Хелео (7:37) при CRF 20 весил бы 244 МБ и просто не залился бы. Поэтому он
собран двухпроходным VBR под целевые ~90 МБ (1450k видео + 128k звук,
maxrate 3200k). Перед выбором проверено сравнением кропов 1:1: **1080p при
1,5 Мбит/с ЧИЩЕ, чем 720p при том же битрейте** — на 720p замыливается фактура
(вафельная ткань халата, кожа), на 1080p она держится. Резать разрешение ради
битрейта на таком материале не надо.

Грабли и проверки:
- **Терра снята в кадре 3,33:1** (контент 3840×1152 внутри 4K-кадра, полосы
  сверху 504 px). `cropdetect` полос НЕ НАШЁЛ (вернул полный кадр) — границы
  замерены построчной яркостью через PIL. Кроп: `crop=3840:1152:0:504`.
- Постер широкой работы делается КРОПОМ 16:9 из середины кадра
  (`crop=1024:576:448:0,scale=1280:720`), а не вписыванием: в разметке карточки
  жёстко стоит width=1280 height=720, и постер 1280×384 врал бы о пропорциях
  (у wedding-td такой постер 1280×534 уже лежит — легаси).
- Постер выбирается по РЕЗКОСТИ, а не на глаз: первый кандидат для РСКГ (58 с)
  попал на смаз проводки, дисперсия градиента 52 против 234 у кадра 12 с.
- Качество веб-копии сверено SSIM против исходника: Терра — 0,991.
- Лупы наведения — прежний канон: 6 с, ширина 720, CRF 30, без звука
  (122–350 КБ; у гонок больше всех из-за движения).

Хранилище `seversvet/site/assets/film` выросло с 647 МБ до 884 МБ. Мягкий предел
репозитория GitHub — 1 ГБ: следующие большие работы уже нужно класть, глядя на
этот счётчик.

## Шоурум продукта `/zakazy-sami.html` (01.09.2026)

Графика страницы НЕ фотографическая и не скриншотная: приборы (лента радара, кольцо месяца,
ряды учёта) рисуются кодом в `assets/css/monolith.css` + `assets/js/monolith.js`. Значит
обновление продукта не требует новых картинок — правится разметка. OG-карточка генерируется:
`python tools/build_og_mono.py` → `assets/og/shtab.jpg`. Шрифт заголовков продукта —
`assets/fonts/Manrope-var-cyr-lat.woff2` (сабсет кириллица+латиница, 32 КБ, собран
`python -m fontTools.subset` из Manrope-Variable приложения).

## Кластер «обучение и вопросы о съёмке» (21.09.2026)

Заведён по прямой просьбе владельца: «чтобы люди заходили на сайт за ответами
на вопросы». Восемь статей, каждая отвечает на один живой запрос и опирается на
факт, который нельзя скопировать из чужой статьи.

| Статья | Свой материал, на котором она стоит |
|---|---|
| `zvuk-na-videosemke` | закон обратных квадратов в числах: 20 × lg(3 ÷ 0,2) ≈ 23,5 дБ разницы между петличкой и камерой; 48 кГц / 24 бита по AES5 |
| `svet-dlya-videosemki` | таблица кельвинов, мерцание 100 Гц от сети 50 Гц и кратные выдержки, CRI против TLCI по EBU Tech 3355 |
| `vertikalnoe-video-kak-snimat` | арифметика кропа: 2160 × 9 ÷ 16 = 1215, то есть из 4K вертикаль терпима, из Full HD (608×1080) нет |
| `kak-podgotovitsya-k-semke` | физика муара: две регулярные решётки (сетка пикселей и мелкий рисунок ткани) дают рябь, которая не чинится ничем |
| `kak-prohodit-videosemka` | смена как неделимая единица; перекрытие ≈ десяток планов на минуту речи; согласующий, добавленный после первой версии, стоит недели |
| `chto-sprosit-u-videografa` | разбор «хороший ответ / плохой ответ» по каждому из 12 вопросов; почему «сколько камер» — бесполезный вопрос |
| `skolko-zanimaet-montazh` | правки идут раундами, и цена раунда почти не зависит от числа замечаний внутри него |
| `montazh-video-s-chego-nachat` | бесплатный Resolve как модель продаж Blackmagic, а не демка; порядок цвета и суждение по Waveform, а не на глаз |

БАЗА ПОД КЛАСТЕР: `EDU_BASE §8м` (звук) и `§8н` (свет) заведены в тот же день.
До этого ни одного факта про звук и свет в базе не было — а это два самых
частых вопроса после цены. Закон прежний: чего нет в базе, того нет в статье.

ОБОЛОЧКА: шапка, подвал и `<style>` новой статьи берутся из живой соседней
страницы скриптом, а не пишутся руками, иначе новая страница расходится с
остальными при первой же правке навигации.

## База знаний: категории вместо списка (21.09.2026, вечер)

Слово владельца: «надо все статьи и обучение разгруппировать красиво по
категориям и сделать удобными… открывая блок статей и обучения, ты мог открыть
то что интересно, а не листать список».

Сделано на трёх хабах одинаково и одним кодом:

- **Полоса «База знаний»** (`.base-nav` в `assets/css/style.css`) — Статьи ·
  Уроки · Инструменты · Занятия, с числом материалов в каждом разделе. Стоит в
  первом экране каждого хаба, текущий раздел помечен `aria-current="page"`.
- **Чипы категорий** (`.hub-nav` + `assets/js/hub.js`) — нажал категорию,
  остальные разделы скрылись. Число материалов на чипе считается из DOM, а не
  проставляется руками: список пополнился — счётчик сам стал другим.
- **Без JS видно ВСЁ.** Разделы прячет только скрипт, поэтому поисковик и
  человек с выключенными скриптами получают полный список. Выбранная категория
  уходит в адрес (`/articles/#remeslo`), ссылку можно дать сразу на неё.

Категории заведены по ВОПРОСУ читателя, а не по истории появления страниц:

| Хаб | Категории |
|---|---|
| `articles/index.html` | Сколько стоит (5) · Как всё проходит (5) · Выбор и договор (3) · Ремесло: свет, звук, кадр (4) · Делаю сам (4) |
| `uroki/index.html` | Цвет (5) · Камера и оптика (3) · Свет и звук (4) |
| `instrumenty/index.html` | Моё приложение (1) · Документы и деньги (8, с 24.09 — КП на видеосъёмку) · План съёмки (4) · Расчёты на площадке (3) · Цвет и приборы (1) |

Из хаба статей УБРАН дубль восьми уроков: они живут в `uroki/`, а в статьях
остался один мост-карточка и три свежих урока. Раньше одни и те же карточки
стояли на двух хабах и конкурировали сами с собой.

## Четыре новые доски-урока (21.09.2026, вечер)

Уроков стало двенадцать. Новые — там, где у нас не было ни одного урока:
звук и свет на площадке.

| Урок | На чём стоит | Чего нет ни у кого |
|---|---|---|
| `petlichka-ili-pushka` | критическое расстояние r_c = 0,057·√(V/T60)·√Q | граница «голос / комната» в метрах и импульсный отклик комнаты |
| `myagkij-svet` | полутень S·L/D, угловой размер 2·arctg(S/2D) | мягкость в градусах и сантиметрах вместо «мягко-жёстко» |
| `balans-belogo` | планковский локус (Kim et al.) → sRGB | видно, что нет такого баланса, при котором обе половины лица нейтральны |
| `mercanie-na-video` | точный интеграл sin² по времени выдержки | полосы считаются по строкам, а не рисуются для вида |

База под них — `EDU_BASE §8о`, источники — `EDU_SOURCES` (раздел «Математика
досок 09–12»). Приёмка — `tools/test_soundboard.py`, `test_softboard.py`,
`test_wbboard.py`, `test_flickerboard.py`: в каждом значение со страницы
сверяется с независимым пересчётом на питоне, а не «похоже на правду».

OG-карточки уроков рисуются `python tools/build_og_uroki.py` → `assets/og/urok-*.jpg`:
справа та же строка показаний, что человек увидит на доске. Числа в карточке —
настоящие значения при состоянии доски по умолчанию, выдумывать их нельзя.

## Три последние доски-урока (21.09.2026, ночь) — серия закрыта

Уроков стало пятнадцать, строк со статусом «план» в `EDU_BASE §8` и `§8в` не
осталось. Последние три закрывают то, что было обещано в карте с августа:

| Урок | На чём стоит | Чего нет ни у кого |
|---|---|---|
| `tri-oshibki-cveta` | кадр считается из линейного света, ошибка — настоящее преобразование кодов | приборы из `scopes-core.js` читают пиксели этого кадра: видно, как кожа уходит с линии 123°, пока серая карта молчит |
| `svet-iz-okna` | площадной источник E = R²/(R²+d²) вместо обратного квадрата | показано, что шаг от окна стоит не две ступени, а меньше вдвое, и почему |
| `raw-ili-log` | кодов на ступень в RAW 12/14 бит, S-Log3 и Rec.709 | видно противоположное общему мнению: в тенях log держит лучше линейного RAW |

База под них — `EDU_BASE §8п`, источники — `EDU_SOURCES` (раздел «Математика
досок 13–15»). Приёмка — `tools/test_errboard.py`, `test_windowboard.py`,
`test_rawlogboard.py`; в каждом значение со страницы сверяется с независимым
пересчётом на питоне, а тест проверен на красное заменой строки в движке.

Хаб уроков: Цвет 7 · Камера и оптика 3 · Свет и звук 5 = 15 карточек.
